part of '../main_page.dart';

class _MainBodySection extends StatelessWidget {
  const _MainBodySection({required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
